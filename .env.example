# Local development (default when unset)
# DATABASE_URL=file:./pickle.db

# Vercel / production — use Turso (https://turso.tech)
# DATABASE_URL=libsql://your-db-name-org.turso.io
# DATABASE_AUTH_TOKEN=your-turso-auth-token
